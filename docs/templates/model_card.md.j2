# Model Card: {{ model_name }} (Run {{ run_id }})

**Version:** {{ version }} | **Date:** {{ timestamp }}

---

## Intended Use

Fraud detection in financial transactions using graph ML. Suitable for:
- Transaction risk scoring (payments, cards, transfers)
- Pre-production research, PoC, and regulatory demonstration
- Audit-ready decision support (not autonomous blocking)

## Out-of-Scope Use

- Real-time streaming (batch-oriented)
- Non-financial domains without retraining
- Autonomous blocking without human review
- Use with unvalidated or adversarial inputs

---

## Training Data

| Attribute | Value |
|-----------|-------|
| Dataset type | {{ dataset_type }} |
| Sample count | {{ n_samples }} |
| Fraud rate | {{ fraud_rate }}% |
| Seed | {{ seed }} |
| Data hash (SHA256) | {{ data_hash }} |

---

## Performance Metrics

| Metric | Value |
|--------|-------|
| PR-AUC | {{ metrics.pr_auc }} |
| Recall @ 1% FPR | {{ metrics.recall_at_1pct_fpr }} |
| Brier (raw) | {{ metrics.brier_raw }} |
| Brier (calibrated) | {{ metrics.brier_calibrated }} |
| ECE (raw) | {{ metrics.ece_raw }} |
| ECE (calibrated) | {{ metrics.ece_calibrated }} |
| Review rate | {{ metrics.review_rate }} |

---

## Ethical Considerations

| Consideration | Status |
|---------------|--------|
| Bias evaluation | {{ fairness.bias_eval }} |
| Fairness metrics | {{ fairness.fairness_metrics }} |
| Environmental impact | {{ optimization.environmental_impact }} |

---

## Limitations

- Synthetic data only (no real PII)
- Temporal leakage mitigated via time-split; rolling evaluation recommended for production
- No adversarial robustness testing
- Fairness not evaluated across demographic groups (synthetic data has no demographics)

---

## Reproducibility

| Artifact | Reference |
|----------|------------|
| Git commit | {{ git_commit }} |
| MLflow run ID | {{ run_id }} |
| Random seed | {{ seed }} |
| Model type | {{ model_type }} |
| Calibrator | {{ calibrator_method }} |

---

## Version History

| Version | Date | Change |
|---------|------|--------|
| {{ version }} | {{ timestamp }} | Initial release |
